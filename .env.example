# Quote form delivery (src/app/api/enquiry/route.ts)
# Without these set, submissions are still accepted but only logged
# server-side, not emailed — safe default for local/staging.

# TODO: get from Nomert — Resend API key (resend.com)
RESEND_API_KEY=

# TODO: get from Nomert — the inbox quote-form enquiries should be sent to
ENQUIRY_TO_EMAIL=
